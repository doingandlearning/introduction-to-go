package catalog

import "testing"

// TODO (Exercise 7): write TestLateFeeTier covering at least the
// boundary days: one just under 7, one at exactly 7, one just under 30,
// one at exactly 30 — assert the exact tier string each time.
func TestLateFeeTier(t *testing.T) {
	t.Skip("TODO: implement TestLateFeeTier")
}

// TODO (Exercise 7): write TestDeskSchedule covering a weekday plus
// both Saturday (6) and Sunday (7) — assert Saturday and Sunday return
// the SAME string, which is only true because of the fallthrough.
func TestDeskSchedule(t *testing.T) {
	t.Skip("TODO: implement TestDeskSchedule")
}
